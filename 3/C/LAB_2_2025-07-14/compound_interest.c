#include <stdio.h>
#include <math.h>

int main(void)
{
  printf("Enter in format (principal,rate,time): ");
  double p,r; int t;
  scanf("(%lf,%ld,%d)",&p,&r,&t);
  double A = p*pow((1+()))
  return EXIT_SUCCESS;
}
