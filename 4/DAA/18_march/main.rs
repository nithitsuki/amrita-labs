// chose pivot like quicksort
// partition into 2 based on pivot (lower & higher)
// only traverse the one needed for smaller/larger
    
fn partition(arr: &mut [i32],k: usize){
    let len: u32 = arr.len();
    let pivot_index: u32 = len - 1;
    let mut i = 0;
    for j in 0..len
        if(arr[j] < )
}

fn main() {
    let mut data = [0,21,132,12,7,67,2,7,3,6];
    println!("Hello world")
}
